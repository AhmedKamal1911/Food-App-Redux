"use server";

import { UpdateProductSchema } from "@/lib/validation/update-product-schema";

export async function updateProduct({ username, img }: UpdateProductSchema) {
  try {
  } catch (error) {}
}
